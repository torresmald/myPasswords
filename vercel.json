{
  "buildCommand": "node mynode.js && npm run build",
  "outputDirectory": "dist/myPasswords",
  "framework": "angular",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
