{
  "buildCommand": "node mynode.js && npm run build",
  "outputDirectory": "dist/myPasswords/browser",
  "framework": "angular",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
