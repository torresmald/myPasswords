{
  "buildCommand": "npm run build",
  "outputDirectory": "dist/myPasswords",
  "framework": "angular",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "builds": [
    {
      "src": "angular.json",
      "use": "@vercel/angular"
    }
  ]
}
